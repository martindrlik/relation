package rex_test

import (
	"testing"
)

func TestProject(t *testing.T) {
	// users := rex.Table{}
	// users.InsertOne(`{"name": "Martin", "age": 39}`)
	// users.InsertOne(`{"name": "Jake", "age": 42}`)
	// age := users.Project("age")
	// ac := dump(append(age.At(0), age.At(1)...)...)
	// ex := dump(float64(39), float64(42))
	// if ac != ex {
	// 	t.Errorf("expected tuple %v, got %v", ex, ac)
	// }
}
